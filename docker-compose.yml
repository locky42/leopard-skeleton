version: '3.8'

services:
  web:
    container_name: ${DOCKER_CONTAINER_NAME}_web
    build:
      context: .
      args:
        XDEBUG_MODE: ${XDEBUG_MODE}
        XDEBUG_START_WITH_REQUEST: ${XDEBUG_START_WITH_REQUEST}
        XDEBUG_CLIENT_HOST: ${XDEBUG_CLIENT_HOST}
        XDEBUG_CLIENT_PORT: ${XDEBUG_CLIENT_PORT}
        XDEBUG_LOG_LEVEL: ${XDEBUG_LOG_LEVEL}
    ports:
      - ${DOCKER_PORT_WEB}
    volumes:
      - .:/var/www/html
      - ./storage/logs:/var/www/html/storage/logs
      - ./storage/database:/var/www/html/storage/database
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html/public
      - XDEBUG_MODE=${XDEBUG_MODE}
      - XDEBUG_START_WITH_REQUEST=${XDEBUG_START_WITH_REQUEST}
      - XDEBUG_CLIENT_HOST=${XDEBUG_CLIENT_HOST}
      - XDEBUG_CLIENT_PORT=${XDEBUG_CLIENT_PORT}
      - XDEBUG_LOG_LEVEL=${XDEBUG_LOG_LEVEL}
    # depends_on:
    #   - db

  # Enable if you want to use MySQL instead of SQLite

  # db:
  #   container_name: ${DOCKER_CONTAINER_NAME}_db
  #   image: mysql:8.4
  #   restart: always
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
  #     MYSQL_DATABASE: ${MYSQL_DATABASE}
  #     MYSQL_USER: ${MYSQL_USER}
  #     MYSQL_PASSWORD: ${MYSQL_PASSWORD}
  #   volumes:
  #     - db_data:/var/lib/mysql
  #   ports:
  #     - ${DOCKER_PORT_DB}

  # Enable if you want to use phpMyAdmin
  
  # phpmyadmin:
  #   container_name: ${DOCKER_CONTAINER_NAME}_phpmyadmin
  #   image: phpmyadmin/phpmyadmin
  #   restart: always
  #   ports:
  #     - ${DOCKER_PORT_ADMINER}
  #   environment:
  #     PMA_HOST: db
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}

  # db:
  #   container_name: ${DOCKER_CONTAINER_NAME}_db
  #   image: postgres:15
  #   restart: always
  #   environment:
  #     POSTGRES_USER: ${POSTGRES_USER}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  #     POSTGRES_DB: ${POSTGRES_DB}
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   ports:
  #     - ${DOCKER_PORT_DB}

volumes:
  db_data:
